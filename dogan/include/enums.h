#pragma once

enum class Resource {
    WOOD,
    SHEEP,
    WHEAT,
    BRICK,
    STONE,
    INVAL
};

enum class Parity {
    EVEN,
    ODD
};
enum class ResourceConfiguration {
    DEFAULT,
};

enum class NumberConfiguration {
    DEFAULT,
};